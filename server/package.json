{
  "name": "pilot-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orchestrator that turns a Linear issue into a PR via headless Claude Code, against real local projects",
  "scripts": {
    "start": "node index.ts",
    "dev": "node --watch index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@linear/sdk": "^89.0.0",
    "ws": "^8.21.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.7.0"
  }
}
